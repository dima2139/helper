import { Button, Input } from "antd";

function Search() {
    return (
        <div>
            <Input size="large" />
            <Button size="large">Search</Button>
        </div>
    );
  }

export default Search